# completion file for bash

_passqr() {
    COMPREPLY=()
    local cur="${COMP_WORDS[COMP_CWORD]}"
    COMPREPLY+=($(compgen -W '-t --timeout' -- "${COMP_WORDS[COMP_CWORD]}"))
    _pass_complete_entries 1
}

complete -o filenames -F _passqr passqr
